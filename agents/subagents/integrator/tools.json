{
  "tools": [
    {
      "name": "mod_suitedash_wire",
      "description": "Create or confirm a SuiteDash client record, optionally wire client portal and billing hooks. ALWAYS include operating_llc, run_id, and dynasty_component in the client's custom fields. NEVER create a duplicate — if the client matches an existing record by email or business name, return the existing record.",
      "input_schema": {"type": "object", "properties": {"business_name": {"type": "string"}, "contact_email": {"type": "string"}, "portal_enabled": {"type": "boolean"}, "billing_tier": {"type": "string"}, "operating_llc": {"type": "string"}, "run_id": {"type": "string"}}, "required": ["business_name", "contact_email", "operating_llc", "run_id"]}
    },
    {
      "name": "mod_acumbamail_wire",
      "description": "Create an Acumbamail list and upload a welcome sequence. ALWAYS use a verified sender identity — attempting to send from an unverified domain fails silently and breaks the verification signal. The welcome sequence must be at least 3 emails.",
      "input_schema": {"type": "object", "properties": {"list_name": {"type": "string"}, "sender_identity": {"type": "string"}, "welcome_sequence": {"type": "array", "items": {"type": "object"}}, "run_id": {"type": "string"}}, "required": ["list_name", "sender_identity", "welcome_sequence", "run_id"]}
    },
    {
      "name": "mod_n8n_import_and_activate",
      "description": "Import a workflow JSON from the run's GitHub repo into n8n, then activate it. ALWAYS use the n8n base at https://n8n.audreysplace.place. The Code node on the Flint VM is sandboxed — workflows that need child_process/fs/os must use SSH or the bridge instead; verify the workflow definition does not rely on forbidden modules before activating.",
      "input_schema": {"type": "object", "properties": {"repo": {"type": "string"}, "workflow_path": {"type": "string"}, "run_id": {"type": "string"}}, "required": ["repo", "workflow_path", "run_id"]}
    },
    {
      "name": "mod_insighto_wire",
      "description": "Create an Insighto chatbot, wire its knowledge base from the run's repo content, test one message round-trip. ALWAYS run a verification fetch after wiring — a 200 on create does not guarantee the bot will respond.",
      "input_schema": {"type": "object", "properties": {"bot_name": {"type": "string"}, "knowledge_source_repo": {"type": "string"}, "run_id": {"type": "string"}}, "required": ["bot_name", "knowledge_source_repo", "run_id"]}
    },
    {
      "name": "mod_callscaler_wire",
      "description": "Wire a CallScaler number. For PA CROP the default is 814-228-2822; new properties get new numbers per the plan. ALWAYS verify inbound routing by triggering a test webhook.",
      "input_schema": {"type": "object", "properties": {"number": {"type": "string"}, "target_webhook_url": {"type": "string"}, "run_id": {"type": "string"}}, "required": ["target_webhook_url", "run_id"]}
    },
    {
      "name": "mod_thoughtly_wire",
      "description": "Wire a Thoughtly voice agent to a provisioned number. Test one call round-trip before marking verified.",
      "input_schema": {"type": "object", "properties": {"agent_id": {"type": "string"}, "number": {"type": "string"}, "run_id": {"type": "string"}}, "required": ["agent_id", "number", "run_id"]}
    },
    {
      "name": "mod_smsit_wire",
      "description": "Wire the SMS-iT flow. Send one test message to a verified internal number (not the customer's) and confirm delivery before marking verified.",
      "input_schema": {"type": "object", "properties": {"flow_name": {"type": "string"}, "test_recipient": {"type": "string"}, "run_id": {"type": "string"}}, "required": ["flow_name", "test_recipient", "run_id"]}
    },
    {
      "name": "mod_trafft_wire",
      "description": "Wire Trafft scheduling. Verify by creating and immediately cancelling one test booking against an internal test slot.",
      "input_schema": {"type": "object", "properties": {"workspace_client_id": {"type": "string"}, "service_name": {"type": "string"}, "run_id": {"type": "string"}}, "required": ["workspace_client_id", "service_name", "run_id"]}
    },
    {
      "name": "mod_seo_wire",
      "description": "Wire SEO: run WriterZen keyword research (or AI fallback), generate 3 SEO blog posts, push them to the run's GitHub repo under content/blog/, and optionally run NeuronWriter content optimization on the top 3 posts. ALWAYS push posts to the repo — a successful generate without a repo commit is not progress. For bilingual markets (detected by project.location) also emit translated titles/descriptions for the secondary language. Do not overwrite existing content/blog/*.md unless the plan item explicitly says so.",
      "input_schema": {"type": "object", "properties": {"repo": {"type": "string"}, "project_name": {"type": "string"}, "project_type": {"type": "string"}, "location": {"type": "string"}, "target_keywords": {"type": "array", "items": {"type": "string"}}, "optimize_with_neuronwriter": {"type": "boolean"}, "run_id": {"type": "string"}}, "required": ["repo", "project_name", "run_id"]}
    },
    {
      "name": "mod_analytics_wire",
      "description": "Wire analytics: create a PostHog project for this run and return the tracking snippet. Plerdy has no REST provisioning API — for Plerdy, return manual setup instructions only, never mark verified. ALWAYS include the PostHog snippet in the submit so the code-generator can inject it; omit the project-side api_token from any log that is written to event-stream (the snippet is embedded in shipped HTML, but full tokens must not appear in event payloads).",
      "input_schema": {"type": "object", "properties": {"project_name": {"type": "string"}, "live_url": {"type": "string"}, "providers": {"type": "array", "items": {"type": "string", "enum": ["posthog", "plerdy"]}}, "run_id": {"type": "string"}}, "required": ["project_name", "providers", "run_id"]}
    },
    {
      "name": "mod_leads_wire",
      "description": "Wire visitor-identification + lead-scoring: register the site with Happierleads and/or Salespanel, retrieve each tracking snippet, and push docs/LEAD-INTELLIGENCE.md to the run's repo with the scoring framework. Salespanel's public API is largely read-only; treat its register call as best-effort and fall back to the tracking-setup doc. A 200 without a returned tracking_code is NOT progress — re-fetch the site/activity by ID before marking verified.",
      "input_schema": {"type": "object", "properties": {"repo": {"type": "string"}, "project_name": {"type": "string"}, "live_url": {"type": "string"}, "providers": {"type": "array", "items": {"type": "string", "enum": ["happierleads", "salespanel"]}}, "run_id": {"type": "string"}}, "required": ["repo", "project_name", "live_url", "providers", "run_id"]}
    },
    {
      "name": "mod_social_wire",
      "description": "Wire Vista Social: create a profile-group for the project and register the repo-generated calendar at social-media/calendar.csv for bulk import. Do NOT upload the calendar via API — Vista Social's bulk scheduler is UI-driven; the submit must name the CSV path in the repo and flag verification=unverified until a human confirms the import. If the Vista Social key is missing, push docs/SOCIAL-MEDIA-SETUP.md with manual instructions instead and return verification=skipped.",
      "input_schema": {"type": "object", "properties": {"repo": {"type": "string"}, "project_name": {"type": "string"}, "calendar_path": {"type": "string"}, "run_id": {"type": "string"}}, "required": ["repo", "project_name", "run_id"]}
    },
    {
      "name": "verify_integration",
      "description": "Generic verification fetch — GET or read-only call to confirm a prior wiring took effect. Use whenever a wire tool's 200 response does not by itself prove the integration works.",
      "input_schema": {"type": "object", "properties": {"module": {"type": "string"}, "external_id": {"type": "string"}, "expected_shape": {"type": "object"}, "run_id": {"type": "string"}}, "required": ["module", "external_id", "run_id"]}
    },
    {
      "name": "emit_halt",
      "description": "Halt per halt-conditions or escalation policy.",
      "input_schema": {"type": "object", "properties": {"run_id": {"type": "string"}, "reason": {"type": "string"}, "context": {"type": "string"}}, "required": ["run_id", "reason"]}
    },
    {
      "name": "submit_results",
      "description": "Return the list of wired integrations per the submit contract in loop.txt.",
      "input_schema": {"type": "object", "properties": {"plan_item_id": {"type": "string"}, "run_id": {"type": "string"}, "integrations": {"type": "array"}, "acceptance_met": {"type": "array"}}, "required": ["plan_item_id", "run_id", "integrations"]}
    }
  ]
}
